@model IEnumerable<HRSW2.Entities.MonthsSalary>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmpInfo)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.EmpInfo.Position.NamePos)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Salary)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EmpInfo.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EmpInfo.PosId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Salary.TotalSalary)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
