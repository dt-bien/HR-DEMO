@model HRSW2.Entities.MonthsSalary

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>MonthsSalary</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="EmpId" class="control-label"></label>
                <select asp-for="EmpId" class ="form-control" asp-items="ViewBag.EmpName"></select>
            </div>
            <div class="form-group">
                <label asp-for="SalaryId" class="control-label"></label>
                <select asp-for="SalaryId" class ="form-control" asp-items="ViewBag.SalaryId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
